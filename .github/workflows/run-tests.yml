name: Tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false

      matrix:
        os: [ubuntu-latest]
        php: [7.3, 7.4, 8.0, 8.1, 8.2]
        laravel: ['8.*', '9.*', '10.*', '11.*', '12.*']
        stability: [prefer-lowest, prefer-stable]

        exclude:
          # prefer-lowest exclusions
          - laravel: '8.*'
            stability: prefer-lowest
          - laravel: '9.*'
            stability: prefer-lowest
          - laravel: '10.*'
            stability: prefer-lowest

          # Laravel 9 requires PHP 8+
          - laravel: '9.*'
            php: 7.3
          - laravel: '9.*'
            php: 7.4

          # Laravel 10 requires PHP 8.1+
          - laravel: '10.*'
            php: 7.3
          - laravel: '10.*'
            php: 7.4
          - laravel: '10.*'
            php: 8.0

          # Laravel 11 requires PHP 8.2+
          - laravel: '11.*'
            php: 7.3
          - laravel: '11.*'
            php: 7.4
          - laravel: '11.*'
            php: 8.0
          - laravel: '11.*'
            php: 8.1

          # Laravel 12 requires PHP 8.2+
          - laravel: '12.*'
            php: 7.3
          - laravel: '12.*'
            php: 7.4
          - laravel: '12.*'
            php: 8.0
          - laravel: '12.*'
            php: 8.1

        include:
          - laravel: '8.*'
            testbench: '6.*'

          - laravel: '9.*'
            testbench: '7.*'

          - laravel: '10.*'
            testbench: '8.*'

          - laravel: '11.*'
            testbench: '9.*'

          - laravel: '12.*'
            testbench: '10.*'

    name: PHP ${{ matrix.php }} - Laravel ${{ matrix.laravel }} - ${{ matrix.stability }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: >
            dom, curl, libxml, mbstring, zip, pcntl,
            pdo, sqlite, pdo_sqlite, bcmath, soap,
            intl, gd, exif, iconv, imagick, fileinfo
          coverage: none

      - name: Setup problem matchers
        run: |
          echo "::add-matcher::${{ runner.tool_cache }}/php.json"
          echo "::add-matcher::${{ runner.tool_cache }}/phpunit.json"

      - name: Install dependencies
        run: |
          TESTBENCH_VERSION="${{ matrix.testbench }}"

          # Testbench 9.0.x falls too low for the Laravel 11 prefer-lowest lane
          # and breaks before the test suite boots.
          if [ "${{ matrix.laravel }}" = "11.*" ] && [ "${{ matrix.stability }}" = "prefer-lowest" ]; then
            TESTBENCH_VERSION="^9.12"
          fi

          composer require \
            "laravel/framework:${{ matrix.laravel }}" \
            "orchestra/testbench:${TESTBENCH_VERSION}" \
            --no-interaction \
            --no-update

          composer update \
            --${{ matrix.stability }} \
            --prefer-dist \
            --no-interaction

      - name: Execute tests
        run: vendor/bin/phpunit
